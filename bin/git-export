#!/bin/bash

if [ -z "$GIT_DIR" ] ; then
  echo "# GIT_DIR is not set or empty"
  exit
fi

echo "# Export your git environment variables by running this string in a bash eval statement"
declare -p GIT_DIR
# TODO: bat can't handle this:
# declare -p GIT_WORK_TREE
