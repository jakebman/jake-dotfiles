#! /bin/bash

if [ "$#" -lt 1 ]; then
	if [ -t 0 ]; then
		echo "please redirect --help output into this program, or specify a program to get an implicit --help. The terminal doesn't make much sense for stdin"
		exit 1
	fi
	bat --language man
else
	man "$@" | bat --language man
fi
