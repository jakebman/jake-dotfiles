
# TODO: consider replacing my ~/bin/git's implicit repo guessing with this
# .env file setting. I don't trust .env files yet, so that's not a thing I want to
# play with yet.
# It would need, *at minimum*, an .mrtrust-like setting
GIT_DIR="${VCSH_REPO_D:-${XDG_CONFIG_HOME:-${HOME}/.config}/vcsh/repo.d}/config.git"
